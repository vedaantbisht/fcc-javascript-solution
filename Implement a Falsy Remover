//Implement a Falsy Remover

function bouncer(arr){
  let result = [];

  for(let i = 0; i < arr.length; i++){
    if(arr[i]){           // truthy check
      result.push(arr[i]);
    }
  }

  return result;
}

//bouncer([7, "ate", "", false, 9]) should return [7, "ate", 9].
