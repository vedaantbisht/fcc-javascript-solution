//Implement an Element Skipper

function dropElements(arr, func){
  let index = arr.findIndex(func);

  if(index === -1){
    return [];
  }

  return arr.slice(index);
}

dropElements([1,2,3,4], n => n >= 3);
dropElements([0,1,0,1], n => n === 1);
dropElements([1,2,3], n => n > 5);
